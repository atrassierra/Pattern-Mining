from population import Population
from individual import Individual
import random
import argparse
import statistics as st

def division(a, b):
    try:
        return float(a / b)
    except ZeroDivisionError:
        return float("inf")

class Elite():
    """
    Elite Class

    This class contains the best individuals generated by the algorithm after g number of generations
    """
    def __init__(self, args, items, generation):
        self.args = args
        self.items = items
        self.generation = generation
        self.elite = dict()
        for firstGroup in self.items:
            for secondGroup in self.items:
                if firstGroup != secondGroup:
                    self.elite[(firstGroup, secondGroup)] = [1] * self.args.elite_length # Initialize the elite population by default

    def isBetter(self): # Para comprobar si un individuo es mejor que otro
        for group in self.elite:
            if self.elite[group] == 0:
                pass

    def update(self):
        pass


    def orderPopulation(self):
        pass